# Combine texts and images to videos

## Install packages
> apt install virtualenv python-pip libmagickwand-dev ffmpeg fonts-wqy-zenhei
> pip install urllib requests wand Pillow imageio python-resize-image
